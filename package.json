{
  "name": "reverie-engine",
  "version": "0.1.0",
  "description": "Reverie: a WebGPU game engine with physically based rendering and AI game generation. Describe a game, play it in seconds.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test tests/*.test.js",
    "shot": "node tools/shot.mjs",
    "playtest": "node tools/playtest.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  }
}
